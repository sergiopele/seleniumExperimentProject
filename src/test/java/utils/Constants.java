package utils;

public interface Constants {
	String CONFIG_FILE_PATH = "src/test/resources/config/config.properties";
	int IMPLICIT_WAIT = 25;
	int EXPLICIT_WAIT = 20;
}
enum KeyWord{
	browser, chrome, firefox, edge, url
}
